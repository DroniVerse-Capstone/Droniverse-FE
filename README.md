#  Drone Simulator với Blockly

Dự án mô phỏng drone 3D kết hợp với Blockly (lập trình kéo thả) để học sinh có thể lập trình điều khiển drone một cách trực quan.

##  Cài đặt và chạy

```bash
# Cài đặt dependencies
npm install

# Chạy development server
npm run dev

# Mở http://localhost:3000
```
